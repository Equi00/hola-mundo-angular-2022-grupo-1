import { MyItineraryPipe } from './my-itinerary.pipe'

describe('MyItineraryPipe', () => {
  it('create an instance', () => {
    const pipe = new MyItineraryPipe()
    expect(pipe).toBeTruthy()
  })
})
